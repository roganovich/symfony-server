<header>
    <h1><a href="{{ path('homepage') }}">Guestbook</a></h1>
    <ul>
        {% for conference in conferences %}
            <li><a href="{{ path('conferences.show', { id: conference.id }) }}">{{ conference }}</a></li>
        {% endfor %}
    </ul>
</header>